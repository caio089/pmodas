🔧 RESOLVER ERRO: "no such table: auth_user"
============================================

❌ PROBLEMA:
O banco de dados SQLite não foi inicializado. O Django precisa criar as tabelas necessárias.

✅ SOLUÇÃO - Execute os comandos abaixo:

1️⃣ ABRIR TERMINAL/PROMPT DE COMANDO:
   - Pressione Win + R
   - Digite: cmd
   - Pressione Enter

2️⃣ NAVEGAR PARA O DIRETÓRIO:
   cd C:\Users\caioc\OneDrive\Documentos\GitHub\pmodas\backend

3️⃣ EXECUTAR MIGRAÇÕES:
   python manage.py migrate

4️⃣ CRIAR USUÁRIO CLIENTE:
   python manage.py shell
   
   No shell do Python, execute:
   from django.contrib.auth.models import User
   User.objects.create_user('cliente', 'cliente@paixao-modas.com', 'pmodas2024')
   exit()

5️⃣ INICIAR SERVIDOR:
   python manage.py runserver

6️⃣ ACESSAR O PAINEL:
   http://127.0.0.1:8000/painel-admin/

📋 CREDENCIAIS:
- Usuário: cliente
- Senha: pmodas2024

⚠️ IMPORTANTE:
- Execute os comandos na ordem correta
- Aguarde cada comando terminar antes do próximo
- Se der erro, verifique se está no diretório correto
